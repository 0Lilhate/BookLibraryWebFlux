spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: daniil
    password: 123
    driver-class-name: org.h2.Driver
    schema: schema.sql
    data: data.sql
    initialization-mode: always

    shell:
      interactive:
        enabled: false



  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

    properties:
      hibernate:
        format_sql: true

    show-sql: true

logging:
  level:
    ROOT: ERROR


